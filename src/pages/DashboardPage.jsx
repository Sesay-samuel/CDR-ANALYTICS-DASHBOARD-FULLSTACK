import { Phone,Clock,CheckCircle2,XCircle,DollarSign,MapPin } from "lucide-react"; import { PieChart,Pie,Cell,ResponsiveContainer,Tooltip } from "recharts";
import Header from "../components/Header"; import Filters from "../components/Filters"; import StatCard from "../components/StatCard"; import CallsChart from "../components/CallsChart"; import RecentCalls from "../components/RecentCalls"; import { formatCurrency,formatDuration } from "../lib/callUtils";
function DashboardPage({calls,searchTerm,setSearchTerm,directionFilter,setDirectionFilter,statusFilter,setStatusFilter,startDate,setStartDate,endDate,setEndDate,resetFilters,setSidebarOpen}) { const totalCost=calls.reduce((s,c)=>s+(Number(c.callCost)||0),0), avg=calls.length?calls.reduce((s,c)=>s+(Number(c.callDuration)||0),0)/calls.length:0, successful=calls.filter(c=>c.callStatus===true).length, failed=calls.filter(c=>c.callStatus===false).length; const cityMap=calls.reduce((a,c)=>{a[c.city||"Unknown"]=(a[c.city||"Unknown"]||0)+1;return a;},{}); const topCities=Object.entries(cityMap).sort((a,b)=>b[1]-a[1]).slice(0,5); const maxCity=Math.max(1,...topCities.map(x=>x[1])); const statusData=[{name:"Successful",value:successful},{name:"Failed",value:failed}]; return <><Header {...{searchTerm,setSearchTerm,setSidebarOpen}}/><Filters {...{directionFilter,setDirectionFilter,statusFilter,setStatusFilter,startDate,setStartDate,endDate,setEndDate,onReset:resetFilters}}/><section className="mt-6 grid gap-4 md:grid-cols-2 xl:grid-cols-5"><StatCard title="Total Calls" value={calls.length} icon={Phone} tone="blue" description="Records in current view"/><StatCard title="Total Call Cost" value={formatCurrency(totalCost)} icon={DollarSign} tone="green" description="API-provided cost"/><StatCard title="Average Duration" value={formatDuration(avg)} icon={Clock} tone="amber" description="Across filtered calls"/><StatCard title="Successful Calls" value={successful} icon={CheckCircle2} tone="violet" description={`${calls.length?Math.round(successful/calls.length*100):0}% success rate`}/><StatCard title="Failed Calls" value={failed} icon={XCircle} tone="rose" description={`${calls.length?Math.round(failed/calls.length*100):0}% failure rate`}/></section><section className="mt-6 grid gap-6 xl:grid-cols-[2fr_1fr_1fr]"><CallsChart calls={calls}/><div className="glass-card rounded-2xl p-5"><h2 className="font-bold text-slate-100">Call Status</h2><p className="text-xs text-slate-400">Successful vs failed</p><div className="relative h-56"><ResponsiveContainer width="100%" height="100%"><PieChart><Pie data={statusData} dataKey="value" innerRadius={58} outerRadius={82} paddingAngle={2}>{statusData.map((_,i)=><Cell key={i} fill={i===0?"#10b981":"#f43f5e"}/>)}</Pie><Tooltip/></PieChart></ResponsiveContainer><div className="pointer-events-none absolute inset-0 flex items-center justify-center"><div className="text-center"><p className="text-2xl font-extrabold text-slate-100">{calls.length}</p><p className="text-[10px] text-slate-400">Total Calls</p></div></div></div><div className="flex justify-center gap-4 text-xs"><span className="text-emerald-600">● Successful {successful}</span><span className="text-rose-600">● Failed {failed}</span></div></div><div className="glass-card rounded-2xl p-5"><div className="mb-4 flex items-center gap-2"><MapPin size={18} className="text-fuchsia-300"/><div><h2 className="font-bold text-slate-100">Top 5 Cities</h2><p className="text-xs text-slate-400">Cities with most calls</p></div></div><div className="space-y-4">{topCities.map(([city,count],i)=><div key={city}><div className="mb-1 flex justify-between text-xs"><span className="max-w-[130px] truncate font-medium text-slate-300">{city}</span><b className="text-slate-100">{count}</b></div><div className="h-2 rounded-full bg-cyan-400/10"><div className={`h-full rounded-full ${["bg-violet-500","bg-cyan-400/100","bg-emerald-400/100","bg-amber-400/100","bg-pink-500"][i]}`} style={{width:`${Math.max(12,count/maxCity*100)}%`}}/></div></div>)}</div></div></section><section className="mt-6"><RecentCalls calls={calls} rowsPerPage={5}/></section></>; }
export default DashboardPage;
